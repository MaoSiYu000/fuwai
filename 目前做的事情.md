# 目前做的事情（用于溯源与阶段记录）

本文件用于记录我们在该项目中**已经发生的改动**，以及每次“总体改动批次”的梳理，便于后续追溯责任与复现。

建议：每完成一个阶段的“总体改动”，就把涉及的文件与目的写到新条目里。

---

## 批次 1：论文与数据集文档扩展、建立ID映射脚本

### 这批做了什么

1. 更新 `0参考论文.md`
   - 调整表格结构：**链接尽量窄**、**主要贡献尽量宽**，把重点放到论文的方法/算法与可复用点上
   - 补充/强化经典与方法论来源：`OULAD`、`Course Signals`、`SHAP`、`SAKT`、`AKT`、`BKT/PFA`
2. 更新 `0数据集.md`
   - 将 `data/data2`、`data/data3` 中新增的 xlsx 表（根据表头判定字段类型）补成文档章节
   - 新增章节编号到后文（包括奖学金、综合测评、毕业去向、课堂考勤、作业提交记录xlsx版、线上学习平台课程学习行为统计、课程信息、跑步打卡、门禁记录，以及 data3 的多张 pre 表）
   - 保持“跨表关联小结”在末尾，并新增了“需要映射的学生标识体系”提示
3. 建立学生统一ID映射脚本
   - 新增 `code/pre/build_id_map.py`：从 `output/pre/学生基本信息_pre.csv` 作为 XH 主表，尝试从 `data/data2`、`data/data3` 的 xlsx 中抽取 `XSBH / SID / USERNUM / cardId / KS_XH ...` 做同值命中匹配，输出 `output/id_map/id_map.csv` 与 `summary.txt`
   - 更新 `code/pre/run_all_pre.py`：把 `build_id_map.py` 加入预处理执行顺序
   - 运行情况：首次版本对超大字段扫描过慢，已在脚本内加入 `MAX_ROWS_PER_FIELD` / `MAX_UNIQUE_PER_FIELD` 限制并重新开始运行

### 涉及文件

- `0参考论文.md`
- `0数据集.md`
- `code/pre/build_id_map.py`
- `code/pre/run_all_pre.py`

### 待确认/待补齐

- `build_id_map.py` 是否已成功生成 `output/id_map/id_map.csv`（需要查看输出与覆盖率摘要）
- 如 `cardId / USERNUM` 不是学号体系，需后续增加更强的映射规则（例如一卡通账号表）

---

## 下一批次（建议）

- 跑通 `build_id_map.py` 并把 `output/id_map/` 结果与覆盖率写进这里
- 若需要，再补充一张“ID映射规则字典/字典表”的文档

---

## 批次 2：data2/data3 批量 xlsx -> csv

### 这批做了什么

1. 新增转换脚本 `code/xlsx_to_csv_data2_data3.py`
   - 扫描 `data/data2`、`data/data3` 下所有 `.xlsx`
   - 只读取每个 xlsx 的第一张 sheet
   - 输出同名 `.csv`（`utf-8-sig` 编码）
   - 写入成功后删除原 `.xlsx`
2. 实际转换结果
   - 共发现 `.xlsx`：18 个
   - 转换成功：18 个
   - 转换失败：0 个
   - 转换后：`data/data2`、`data/data3` 各剩 9 个 `.csv`，`.xlsx` 归零

### 涉及文件

- `code/xlsx_to_csv_data2_data3.py`
- `data/data2/*.xlsx` / `data/data3/*.xlsx`（已删除，替换为同名 csv）

---

## 批次 3：整理预处理结果为 data/data1

### 这批做了什么

1. 建立清洗后数据目录 `data/data1`
2. 将 `output/pre/*.csv` **复制**到 `data/data1/`
   - 目的：把“已清洗数据”作为后续建模/聚类的统一数据源入口（你负责的模式挖掘可直接从 data1 读取）

### 结果

- `output/pre` 共 9 个清洗结果 csv
- `data/data1` 已同步 9 个 csv（文件名保持 `_pre.csv` 后缀）

---

## 批次 4：EDA（多表特征草稿）与聚类前准备流水线

### 这批做了什么

1. 建立 EDA 模块（专用代码与专用输出）
   - 新增 `1EDA.md`：EDA 操作手册 + 方式与结果记录（含“学期级 vs 总体级”策略）
   - 新增 `code/EDA/run_eda.py`：自动扫描 `data1/data2/data3`，构建学生-学期特征草稿并输出到 `output/eda/`
   - EDA 输出：`table_overview.csv`、`features_student_term_draft.csv`、`feature_ranges.csv`、`corr_matrix.csv`、`k_screening_metrics.csv`、`k_stability.csv`
2. EDA关键口径落地（为避免翻车的保守策略）
   - **作业耗时修正**：time-only 且负差按“跨1小时”修正（+3600 秒），得到更合理的作业耗时分布
   - **体能/体测对齐**：体能考核 `XQ` 解析到 `TERM_KEY`；体测数据 `TCNF` 保守映射到学年第2学期
   - **学期级 vs 总体级**：能对齐学期就做 `XH+TERM_KEY`；对齐后关键字段全空则降级为 `XH` 总体特征（综合测评已按此降级）
3. 新增“聚类前准备”模块（模块化脚本，输出隔离）
   - 新增 `2聚类前准备.md`：聚类前准备清单与运行方式
   - 新增 `code/cluster_prep/`：00~05 分步脚本 + `run_all.py` 串联
   - 输出到 `output/cluster_prep/`：冻结输入、缺失指示+填补、winsorize+robust scaling、冗余删除、异常标注、PCA检查等

### 阶段产物（你后面聚类直接用哪个）

- **EDA草稿特征表**：`output/eda/features_student_term_draft.csv`
- **聚类前准备最终输入（推荐）**：`output/cluster_prep/04_features_outliers_flagged.csv`

### 涉及文件

- `1EDA.md`
- `code/EDA/run_eda.py`
- `2聚类前准备.md`
- `code/cluster_prep/*.py`
- `目前做的事情.md`（本条记录）

---

## 批次 5：聚类主线升级为“两层软聚类（mode+subtype）”

### 这批做了什么

1. 聚类从“单层 cluster”升级为“两层结构”
   - **mode 层**：先用稳定维度分数做软聚类
   - **subtype 层**：在每个 mode 内再细分子类
2. mode 候选范围从 `4..8` 扩展到 `4..12`（历史探索阶段）
   - 当时由模型按 BIC 自动选择为 `K=10`
3. 产出解释材料
   - mode 维度画像
   - subtype Top 指标差异表
   - subtype 命名草案

### 关键产物

- `output/cluster/student_term_modes_and_subtypes.csv`
- `output/cluster/mode_model_selection.csv`
- `output/cluster/subtype_model_selection.csv`
- `output/cluster/mode_profile_dims.csv`
- `output/cluster/subtype_top_features.csv`
- `output/cluster/subtype_name_draft.csv`
- `4当前模式说明.md`（该批次当时同步到 mode=10，后续已在批次7回切为 mode=8）

---

## 批次 7：最终口径回切到 mode=8（稳定性优先）

### 这批做了什么

1. 按你的最终决定，把 mode 固定到 `K=8`
   - 修改 `code/cluster/run_gmm_modes_and_subtypes.py`（`MODE_K_LIST=[8]`）
2. 重新跑两层软聚类与解释材料
   - 重算 mode/subtype 主表、mode 画像、subtype 证据与命名草案
3. 同步更新关联文档
   - `3聚类.md`、`4当前模式说明.md`、`00方案选择.md`

### 关键产物

- `output/cluster/student_term_modes_and_subtypes.csv`（已是 mode=8）
- `output/cluster/mode_model_selection.csv`（当前仅保留 K=8 口径）
- `output/cluster/mode_profile_dims.csv`
- `output/cluster/subtype_top_features.csv`
- `output/cluster/subtype_name_draft.csv`
- `3聚类.md`（结论与口径改为 mode=8）
- `4当前模式说明.md`（标题、表格、说明改为 mode=8）
- `00方案选择.md`（当前版本状态改为 mode=8）

---

## 批次 8：软聚类 K 值稳定性评估（mode 层）

### 这批做了什么

1. 新增软聚类评估脚本：`code/cluster/evaluate_soft_clustering_stability.py`
2. 对 mode 层执行 `K=4..12` 多随机种子评估（不只看 BIC）
   - 拟合：BIC/AIC
   - 硬标签稳定性：ARI/NMI
   - 软概率稳定性：概率向量 L1/JSD
   - 不确定性：pmax/entropy
3. 将结论同步到 `3聚类.md`

### 关键产物

- `output/cluster/soft_k_evaluation_mode.csv`
- `output/cluster/soft_k_pairwise_details_mode.csv`
- `3聚类.md`（新增“7.2 软聚类 K 值评估”）

### 结果结论（简版）

- `K=6~8` 稳定性较好，`K>=9` 稳定性明显下降。
- 最终展示版继续采用 `mode=8`（稳定性与解释性折中更优）。

---

## 批次 9：新增稳健数据补齐模块（独立目录）

### 这批做了什么

1. 新增补齐方案文档：`1数据补齐.md`
2. 新增代码目录：`code/data_impute/`
   - `00_missing_audit.py`（缺失审计）
   - `01_safe_impute.py`（分层稳健补齐：XH中位数 -> TERM_KEY中位数 -> 全局中位数）
   - `02_impute_impact_report.py`（补齐前后分布位移评估）
   - `run_all.py`（一键运行）
3. 新增输出目录：`output/data_impute/`
   - 已生成审计、补齐日志、补齐后特征、影响评估表

### 关键产物

- `1数据补齐.md`
- `code/data_impute/00_missing_audit.py`
- `code/data_impute/01_safe_impute.py`
- `code/data_impute/02_impute_impact_report.py`
- `code/data_impute/run_all.py`
- `output/data_impute/00_missing_audit.csv`
- `output/data_impute/00_feature_summary.csv`
- `output/data_impute/01_features_imputed_safe.csv`
- `output/data_impute/01_impute_log.csv`
- `output/data_impute/02_impute_impact_report.csv`

### 风险控制口径（简版）

- 保留 `is_missing_*` 指示列，避免丢失“缺失本身”的信息。
- 对高缺失特征做 `high_missing_feature` 标记，不直接当核心决策特征。
- 增加补齐影响位移告警（均值/标准差/中位数/P90 相对变化），避免补齐造成严重偏差。

---

## 批次 10：补齐前后聚类对照实验（mode=8）

### 这批做了什么

1. 新增对照脚本：`code/data_impute/03_compare_cluster_before_after.py`
2. 在同样 `mode=8` 条件下，跑两版对照：
   - baseline（当前主线）
   - imputed（补齐增强版）
3. 对比输出稳定性、软概率差异、不确定性、簇规模均衡性，并生成汇总表
4. 将“影响与结论”同步写入 `1数据补齐.md`

### 关键产物

- `code/data_impute/03_compare_cluster_before_after.py`
- `output/data_impute/compare/cluster_compare_metrics.csv`
- `output/data_impute/compare/cluster_compare_summary.csv`
- `output/data_impute/compare/imputed_student_term_modes_and_subtypes.csv`
- `output/data_impute/compare/imputed_soft_k_evaluation_mode.csv`
- `1数据补齐.md`（新增第6节：影响记录 + 对照结果 + 结论）

### 结果结论（简版）

- BIC 虽更低（拟合提升），但稳定性明显下降：
  - `ARI: 0.9529 -> 0.7563`
  - `NMI: 0.8663 -> 0.7823`
  - `prob_L1: 0.01948 -> 0.06878`（变差）
  - `prob_JSD: 0.02968 -> 0.16177`（变差）
- 最大簇占比从 `35.70%` 升到 `47.35%`，簇均衡性变差。
- 结论：当前补齐版**无实质提升**，主线继续用 baseline（mode=8）。

---

## 批次 11：补齐策略 V2（白名单补齐）+ 再对照

### 这批做了什么

1. 新增白名单补齐脚本：`code/data_impute/01b_whitelist_impute.py`
2. 新增 V2 对照脚本：`code/data_impute/03b_compare_cluster_before_after_whitelist.py`
3. 自动筛选补齐白名单（中低缺失+高可解析+足够区分度），并重跑 mode=8 对照
4. 将结果写入 `1数据补齐.md`（第7节）

### 关键产物

- `output/data_impute/01b_impute_whitelist.csv`
- `output/data_impute/01b_features_imputed_whitelist.csv`
- `output/data_impute/01b_impute_log.csv`
- `output/data_impute/compare_v2/cluster_compare_v2_metrics.csv`
- `output/data_impute/compare_v2/cluster_compare_v2_summary.csv`

### 结果结论（简版）

- 白名单特征数：`11`
- 核心结果与 V1 基本一致：
  - `BIC: 62489.44 -> 56337.24`（更低）
  - `ARI: 0.9529 -> 0.7563`（下降）
  - `NMI: 0.8663 -> 0.7823`（下降）
  - `prob_L1: 0.01948 -> 0.06878`（变差）
  - `prob_JSD: 0.02968 -> 0.16177`（变差）
  - 最大簇占比：`35.70% -> 47.35%`（更不均衡）
- 结论：V2 仍无实质提升，主线继续保持 baseline（mode=8）。

---

## 2026-04-07 批次 12：聚类脚本重构（n_init 分层）+ 群体画像可视化 + 软聚类本质说明

### 这批做了什么

1. **群体画像可视化**
   - 新增 `code/cluster/visualize_group_profiles.py`：生成6张群体画像分析图
   - 输出到 `output/cluster/after/`（含 README.md 说明每张图的含义与读法）
   - 6 张图：整体 mode 分布、各学院 mode 堆叠条、各学院维度热图、Top25 专业 mode 堆叠条、8 mode 维度画像、学院风险占比

2. **GMM n_init 改进实验**
   - 尝试将 `n_init` 从 3 提高到 10，以期改善 subtype 层小样本稳定性
   - **实验发现**：n_init=10 在 mode 层找到了不同局部最优（非纯标签交换），M1 的关键特征"高作业投入"消失，可解释性变差；pmax 也略有下降
   - **结论**：mode 层原始解（n_init=3, ARI=0.95）是已知最优可解释解，不应重跑
   - **代码重构**：`_fit_best_gmm` 改为接受 `n_init` 参数，mode 层仍用 `n_init=3`，subtype 层改为 `n_init=10`
   - **output 文件未变**：从备份恢复原始解，所有下游文件未更新

3. **文档更新**
   - `3聚类.md`：新增 7.3 节，记录 n_init 实验过程与结论，并写明 re-run 警告
   - `目前做的事情.md`：本条记录

### 关键产物

- `code/cluster/visualize_group_profiles.py`（新增）
- `output/cluster/after/*.png`（6 张图，新增）
- `output/cluster/after/README.md`（各图说明，新增）
- `code/cluster/run_gmm_modes_and_subtypes.py`（`_fit_best_gmm` 重构，mode n_init=3，subtype n_init=10）
- `3聚类.md`（新增 7.3 节）

### 注意

- output/cluster/student_term_modes_and_subtypes.csv 及 front/data/ 内容**未变**，前端数据无需更新

---

## 批次 6：输出数据文档化 + 群体画像补齐班级维度

### 这批做了什么

1. 给 `output/` 各主要目录补 `README.md`
   - 说明每个文件夹/表格的用途、字段含义、给谁用
2. 群体画像补齐班级维度
   - 从带班级字段的表（签到/作业）提取 `CLASS_ID/CLASS_NAME`
   - 通过 `LOGIN_NAME/CREATER_LOGIN_NAME` 对齐学号后聚合
   - 成功生成 `group_profile_by_class.csv`

### 关键产物

- `output/README.md`
- `output/cluster/README.md`
- `output/cluster_prep/README.md`
- `output/eda/README.md`
- `output/pre/README.md`
- `output/id_map/README.md`
- `output/cluster/group_profile_by_class.csv`

